/* =========================================================
   PASTE YOUR SUPABASE INFO HERE
   ---------------------------------------------------------
   1. Make a free account at https://supabase.com
   2. Open your project → Settings (gear) → Data API
   3. Copy "Project URL"  → paste between the quotes on SUPABASE_URL
   4. Copy the "anon public" API key → paste it on SUPABASE_ANON_KEY
   (Full step-by-step is in SETUP.md)

   Until you fill these in, the app still works — it just saves
   only on this one computer instead of sharing with everyone.
   ========================================================= */

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
